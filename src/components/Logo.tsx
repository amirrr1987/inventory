const Logo = (props: any) => {
  return (
    <img
      className={props.className + " rounded w-full"}
      src="https://picsum.photos/id/567/300/100"
      alt=""
    />
  );
};
export default Logo;
